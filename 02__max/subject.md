# max

## Assignment

Write a function that receives an array of integers and its number of elements, and returns the largest number in the array. If the array is empty, return `0`.

## Prototype

```c
int max(int *tab, unsigned int len);
```

## Expected file

```text
max.c
```

## Allowed functions

```text
None
```
